workspace(name = "physx")

load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

# Bazel override_repository
# For using physx from applied2 without making applied2 download physx each time (refer to physx locally, so I can patch it myself. Will use BUILD files in my local physx, but not the WORKSPACE).
#https://docs.bazel.build/versions/master/external.html#overriding-repositories-from-the-command-line
